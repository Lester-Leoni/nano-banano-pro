# Pinned runtime dependencies for predictable installs.
#
# NOTE: This file keeps runtime deps minimal. Optional dependencies are in
# requirements-optional.txt.

streamlit==1.53.0

deep-translator==1.11.4
